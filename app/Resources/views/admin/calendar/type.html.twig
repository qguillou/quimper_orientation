{% extends 'base_admin.html.twig' %}

{% block body %}
<div class="col-md-12">
<h1>Administration des types de course</h1>
</div>

<div class="col-md-3">
  <div class="list-group">
    <a href="/admin/type" class="list-group-item {% if form.vars.data.id == null %}active{% endif %}">
      <i class="fa fa-plus-circle"></i> Nouveau type
    </a>
    {% for type in types %}
    <a href="/admin/type/{{type.getId()}}/" class="list-group-item {% if form.vars.data.id == type.getId() %}active{% endif %}">
      {{type.getNom()}}
    </a>
    {% endfor %}
  </div>
</div>

<div class="col-md-1">
</div>

<div class="col-md-6">
  <div class="row">

    {% include 'alerts/alerts.form_validation.html.twig' %}

    {{ form_start(form, {'attr': {'class': 'form-horizontal'}}) }}
    <fieldset>
      <h2>Modification du type</h2>
      <p class="lead">N'oubliez pas d'enregistrer !</p>
      {{ form_errors(form) }}

      <div class="row">
        <div class="form-group">
          {{ form_label(form.nom) }}

          <div class="col-sm-6">
            {{ form_widget(form.nom) }}
            {{ form_errors(form.nom) }}
          </div>
        </div>
      </div>

      <div class="row">
        <div class="form-group">
          {{ form_label(form.color) }}

          <div class="col-sm-6">
            {{ form_widget(form.color) }}
            <span class="help-block">
              Permet de personnaliser la couleur d'affichage dans le calendrier, noir par défaut si non renseigné.
              <br>
              La couleur est au format héxadécimal exemple: #68b211
            </span>
            {{ form_errors(form.color) }}
          </div>
        </div>
      </div>

      <div class="row">
        {{ form_widget(form.reset) }}
        {{ form_widget(form.save) }}
        {% if form.vars.data.id %}
        {{ form_widget(form.delete) }}
        {% endif %}
      </div>
    </fieldset>
    {{ form_widget(form, {'attr': {'class': 'hidden'}}) }}
    {{ form_end(form) }}
  </div>
</div>
{% endblock %}

{% block stylesheets %}
{% endblock %}
