{% extends 'AdminBundle::form_default.html.twig' %}

{% block modal_title %}Édition d'une carte{% endblock %}

{% block modal_body %}
<div class="row">
    <div class="form-group">
        {{ form_label(form.display) }}
        <div class="col-sm-9">
            {{ form_widget(form.display) }}
            {{ form_errors(form.display) }}
        </div>
    </div>
</div>

<div class="row">
    <div class="form-group">
        {{ form_label(form.nom) }}
        <div class="col-sm-9">
            {{ form_widget(form.nom) }}
            {{ form_errors(form.nom) }}
        </div>
    </div>
</div>

<div class="row">
    <div class="form-group">
        {{ form_label(form.alert) }}
        <div class="col-sm-9">
            {{ form_widget(form.alert) }}
            {{ form_errors(form.alert) }}
        </div>
    </div>
</div>
{% endblock %}

{% block path_add %}"{{ path('carte_add') }}"{% endblock %}
